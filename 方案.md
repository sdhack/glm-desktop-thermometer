# GLM 桌面温度计 —— 技术方案（规划稿 v1.0）

> 目标：钉在桌面右上角的半透明系统监控小部件，展示 CPU/GPU/内存等的使用率与温度；
> 全屏游戏时仍可见，全屏看视频时自动隐藏；低内存、高稳定、开机自启、视觉不突兀。

---

## 1. 需求拆解与"举一反三"

原始需求 → 泛化为一条**显隐策略规则引擎**，核心规则一句话：**永远置顶在所有窗口之前，唯一的例外是全屏看视频时自动隐藏**：

| 场景 | 窗口状态 | 表现 |
|---|---|---|
| 桌面/普通应用 | 常规 | 右上角常显、置顶，半透明，鼠标穿透 |
| 任何非全屏窗口（含 maximized 窗口） | 前台 | 置顶浮在窗口之上 |
| 全屏游戏（无边框/独占全屏） | 全屏前台 | 置顶显示（独占全屏需 D3D Hook，见 §5.3） |
| 全屏视频（浏览器/播放器） | 全屏前台 + 媒体播放中 | **自动隐藏**（唯一例外） |
| 后台挂机游戏 | 后台 | 置顶常显 |
| 用户临时拖动/设置 | 鼠标进入悬浮区 | 解除穿透，可拖拽；离开后恢复穿透 |
| 游戏内全屏直播/录制 | — | 置顶显示 |

关键洞察：**置顶是默认态，只有"全屏 + 媒体播放"这一个信号组合才触发隐藏**。判断"是否在看视频"用信号融合（是否正在渲染自己的画面 vs 是否是被动媒体播放），而不是维护进程白名单/黑名单。

---

## 2. 总体架构（三进程隔离）

```
┌────────────────────────────────────────────────────┐
│  sensor-agent.exe（采集进程，Low IL，独立崩溃域）      │
│  - CPU: NtQuerySystemInformation / PDH             │
│  - 内存: GlobalMemoryStatusEx                       │
│  - GPU: NVAPI(N卡) / ADLX(A卡) / PDH GPU Engine     │
│  - 温度: LHM 内核驱动(WinRing0替代) / NVAPI / ADLX   │
│  - 硬盘: NVMe/SMART 健康日志(免驱动)                 │
│  - 风扇: SuperIO(LHM/WinRing0) + NVML/ADLX(显卡风扇) │
│  - 采样: 1000ms 基础 + 500ms 游戏)，输出共享内存环形缓冲 │
└──────────────┬─────────────────────────────────────┘
               │ 共享内存(MemoryMappedFile) + 事件通知（无 IPC 拷贝）
┌──────────────▼─────────────────────────────────────┐
│  tempmon.exe（UI/策略进程）                          │
│  - Win32 分层窗口 + DirectComposition 渲染          │
│  - 显隐策略引擎（PresentMon/SMTC 信号融合）           │
│  - 托盘、设置、开机自启管理                           │
└──────────────┬─────────────────────────────────────┘
               │ 命名管道控制
┌──────────────▼─────────────────────────────────────┐
│  overlay-hook.dll（可选，Phase 2）                   │
│  - 远程注入独占全屏游戏进程，挂 D3D11/12 Present      │
│  - 从共享内存读数据，游戏画面内直接绘制               │
└──────────────────────────────────────────────────────┘
```

**为什么隔离**：传感器读取涉及内核驱动与各厂商 SDK，是崩溃高发区。采集进程崩溃后由 UI 进程的看门狗在 2 秒内拉起，UI 与已显示数据不受影响，达到"永不闪退"的稳定性目标。

---

## 3. 技术选型（对比后推荐）

| 维度 | A. Rust + Win32/DComp（推荐） | B. C#/.NET + LHM + WPF | C. Tauri/Web 前端 | D. C++ 全家桶 |
|---|---|---|---|---|
| 内存占用 | ★★★★★ ~10–20MB | ★★★ ~80–150MB | ★★ ~120–250MB | ★★★★★ |
| 开发效率 | ★★★ | ★★★★★ | ★★★★ | ★★ |
| 渲染流畅度/透明合成 | ★★★★★ DComp 硬件合成 | ★★★★ | ★★★ | ★★★★★ |
| 生态 | sysinfo/nvapi-rs 成熟 | LHM 现成、温度最全 | 前端 UI 省事 | 一切手写 |
| 长期稳定性 | 编译期保证+无 GC 停顿 | GC 偶发卡顿 | WebView2 更新风险 | 手写错误多 |

**推荐：方案 A 为主体（Rust）**，理由：
1. 常驻内存 10–20MB、CPU <0.5%，无 GC、无 WebView2 运行时，符合"内存占用低 + 稳定性强 + 前沿"。
2. `windows-rs` 官方 crate 可直接操作 DComp/DXGI/SMTC；`nvapi-rs`、`sysinfo`、PDH 绑定齐全。
3. 温度采集这一块生态例外：**复用 LibreHardwareMonitor 的内核驱动思路**（Phase 1 用 `nvapi-rs`/`ADLX` 拿 GPU 温度 + `WinRing0` 驱动拿 CPU 温度；若嫌驱动签名麻烦，Phase 1 可先以进程方式调用 LibreHardwareMonitor.exe 的 HTTP/共享内存接口，Phase 2 原生化）。

技术栈清单：
- 语言/构建：Rust 2024 edition + `windows-rs`（微软官方维护）
- 窗口：Win32 分层窗口 `WS_EX_LAYERED | WS_EX_TOPMOST | WS_EX_TOOLWINDOW | WS_EX_NOACTIVATE | WS_EX_TRANSPARENT`（鼠标穿透）
- 渲染：**DirectComposition + IDCompositionDevice2**，逐像素 alpha，DWM 硬件合成，脏区最小重绘；文本用 DirectWrite
- 全屏检测：**PresentMon 风格 ETW 监听**（`PresentMon` 开源，MIT，Intel 维护）检测前台进程的 Present 模式（Composed/Composed_Composition/Hardware_Legacy_Copy 等）判断独占/无边框全屏
- 媒体检测：`GlobalSystemMediaTransportControlsSessionManager`（WinRT，系统级"正在播放"信息）
- GPU 数据：占用率、**显存占用率**与温度同组展示 —— NVIDIA → `NVAPI`/NVML；AMD → `ADLX`；Intel/兼容兜底 → PDH `\GPU Engine(*)\Utilization Percentage`；**显存占用率**：专用显存用量/总显存（NVML、ADLX、PDH `\GPU Adapter Memory`），多 GPU 时取当前活跃独显
- CPU 温度：WinRing0x64 内核驱动读 MSR（LibreHardwareMonitor 同款）或 LHM 外部进程兜底
- 自启：**任务计划程序（登录触发 + 延迟 30s）**，优先于 HKCU\Run（可带延迟、最高权限可选）
- 单实例：命名互斥体；崩溃恢复：UI 进程 Job Object + 看门狗自动重启采集进程

---

## 4. UI / 交互设计（不突兀）

- 位置：默认吸附屏幕右上角，留 8px 安全边距；多显示器时可选"主屏"或"鼠标所在屏"
- 三种显示状态（可自由切换）：
  - **常显（默认）**：单行信息带，约 360×26px，类似状态栏，视觉轻
  - **收起**：约 36×36px 的极简小胶囊，只显示当前最高温度 + 小色点（绿/橙/红指示健康状态），不挡视线；点击胶囊恢复单行
  - **悬停展开**：鼠标悬停在信息带上时，下方弹出 360×96 历史曲线迷你图，移开即收
- 透明度：默认 70%，提供 40%–95% 滑杆；**检测到深/浅壁纸自动选黑/白主题**（读桌面壁纸平均亮度，举一反三的细节）
- 布局（常显态，单行，按可获取到的数据动态增减段位）：
  ```
  CPU 45% 62° │ GPU 32% VR 42% 55° │ MEM 58% 45° │ SSD 43° HDD 38° │ FAN 1200 1800 900
  ```
  - GPU 段：核心占用、显存占用（VR，紫色）、温度
  - MEM 段：使用率 + 内存温度（读得到才显示）
  - SSD/HDD 段：各盘温度，最多并列 2 块盘，更多盘合并入悬停展开
  - FAN 段：各风扇转速（RPM），只显示可读的风扇，最多 3 个，其余进悬停展开
  - **设计铁律：无数据不占位**——读不到温度/转速的项整段隐藏，段位顺序可拖动、可在设置中开关
  - 单行超宽时按用户设置的优先级自动折叠低优先级段位进悬停展开，保证不超屏
  - 各段用竖线分隔，字号 11–12px，数字右对齐避免跳动（等宽数字字体）；温度超阈值时变色（62° 橙→红）；核心/显存用颜色区分（核心蓝、显存紫），悬停有 Tooltip，展开态历史曲线配色与单行一致

  收起态（小胶囊）：
  ```
  (●) 62°
  ```
  色点取 CPU/GPU 温度最高者的健康档位，温度值同色。
- 交互：默认鼠标穿透不挡操作；按住 `Ctrl` 悬停解除穿透 → 可拖拽/滚轮调透明度/右键菜单
- 设置入口（无托盘）：按住 Ctrl + 右键点击悬浮条弹出菜单——收起为小胶囊、透明度 50/70/90、开机自启开关、退出；Ctrl+点击胶囊展开
- 高温告警：温度超阈值时对应行变橙/红并短暂提升不透明度（这是"信息"，不做弹窗打扰）
- 性能档位：节能 2s / 均衡 1s / 游戏 500ms（进全屏游戏自动切游戏档，退出恢复）

---

## 5. 关键技术难点与解法

### 5.1 半透明置顶且不抢焦点
分层窗口 + DComp 逐像素 alpha（非 color-key），DWM 硬件合成零 CPU 混合成本。`WS_EX_NOACTIVATE + WS_EX_TOOLWINDOW` 保证永不抢焦点、不出现在 Alt+Tab/任务栏。置顶丢失时监听 `EVENT_SYSTEM_FOREGROUND` 钩子即时纠正。

### 5.2 鼠标穿透与可设置态共存
常态 `WS_EX_TRANSPARENT` 整窗穿透；按住 Ctrl 时移除该扩展样式进入"编辑态"（拖拽位置、调整大小）。这是 MSI Afterburner 等工具未做好的体验点。

### 5.3 全屏游戏可见（本项目最大难点）
- **无边框全屏（含绝大多数现代游戏默认）**：Topmost 分层窗口天然可见，DWM 合成不排除非全屏窗口 → 零成本覆盖。
- **独占全屏（Exclusive Fullscreen）**：Windows 会绕过 DWM，普通置顶窗口不可见。两条路线：
  - Phase 1 策略：通过 ETW/PresentMon 检测到独占全屏游戏时，**自动建议/一键切换游戏为无边框**（很多游戏支持 -borderless 参数或设置），并在 HUD 上提示；
  - Phase 2 完全体：`overlay-hook.dll` 注入（全局 Windows Hook `SetWindowsHookExW` 落地 DLL + Detours 挂 `IDXGISwapChain::Present`），在游戏自己的交换链上直接绘制 OSD（RTSS 同款技术）。共享内存只读数据，注入层零配置、崩溃不影响游戏（SEH 包裹，挂掉自动卸载）。
- 反作弊说明：RTSS 类注入在高危反作弊（Vanguard/ACE）下有风险，方案默认只做"提示切无边框"，注入为可选高级功能并在文档中明示。

### 5.4 全屏视频自动隐藏（唯一隐藏条件）
置顶为常态：`WS_EX_TOPMOST` 永远保持，且监听 `EVENT_SYSTEM_FOREGROUND` 钩子，一旦有其他 topmost 窗口出现立即重新置顶，保证始终浮在所有窗口之前。唯一例外是全屏视频，信号融合，任一满足即隐藏：
1. 前台窗口为已知播放器/浏览器且处于全屏，且 SMTC 会话播放状态 = Playing；
2. 恢复显示：前台离开全屏 或 SMTC 变 Paused/Stopped 超过 5s。

### 5.5 温度/转速数据的可靠性（前提：能获取到才显示）
- GPU 温度：NVAPI `NVAPI_MAX_THERMAL_SENSORS`（GPU/热点/显存），ADLX 同理 —— 无需驱动，**优先走这条**
- CPU 温度：Windows 无公开 API，必须内核驱动（WinRing0）读 MSR/EC。方案：驱动文件随程序安装到 ProgramData，按需加载；读取失败自动降级为"仅显示使用率"
- **内存温度**：DDR5/部分 DDR4 内存条带 SPD 温度传感，经 SMBus 读取（LibreHardwareMonitor 已实现同款逻辑，经 LHM 桥接或 WinRing0 访问 SMBus）；台式机部分主板经 EC 也可读到 DIMM 温度。读不到就隐藏（多数笔记本/入门内存无此传感）
- **硬盘温度**：M3 实测定案——本机 Insider 预览版（28000）的 stornvme 对协议查询返回全零、温度属性报 NOT_SUPPORTED，故采用与存储栈同源的 `Get-StorageReliabilityCounter`（PowerShell 管道）由采集子进程**低频轮询（60 秒）+ 缓存**，免驱动且全盘型覆盖；预留 NVMe 健康日志/SMART 兜底路径（零售版系统可用）
- **风扇转速**：M4 定案——经 lhm-bridge（LibreHardwareMonitorLib）读取 SuperIO/显卡风扇；本机为 Nuvoton NCT6798D（Fan#2 ≈1900rpm、Fan#5 ≈1200rpm）。CPU 温度同源（各核心/Package 取最高）。选桥接而非 WinRing0 内核驱动的原因：本机装有 360 全家桶，内核驱动大概率被拦截误报；.NET 桥接进程约 73MB 常驻但完全隔离在采集侧
- 每个传感器上线时先做"可用性探测"（3 次采样成功率与数值合理性校验），不可用即永久隐藏该项并记日志，不做无意义的轮询，省 CPU

### 5.6 低内存低 CPU 清单
- 采样定时器用 `CreateWaitableTimerEx`（高精度低抖动），空闲档 2s
- 渲染只在数据变化时提交脏区（`IDCompositionTarget` 局部更新），空闲时 CPU ≈ 0
- 共享内存 + 命名事件做进程间数据传递，零序列化开销
- 日志环形缓冲 + 级别控制，默认 Info，无文件 IO 热路径

---

## 6. 目录结构（规划）

```
GLM桌面温度计/
├─ crates/
│  ├─ tempmon-ui/        # UI+策略进程（winit/DComp 手写窗口）
│  ├─ tempmon-sensor/    # 采集进程
│  ├─ sensor-nvapi/      # NVAPI/NVML 绑定封装
│  ├─ sensor-adlx/       # AMD ADLX 封装
│  ├─ sensor-lhm-bridge/ # LibreHardwareMonitor 桥接（Phase 1 兜底）
│  ├─ fullscreen-detect/ # PresentMon ETW + SMTC 封装
│  └─ shared-protocol/   # 共享内存布局、版本、序列化
├─ overlay-hook/         # Phase 2 C++/Rust DLL（D3D hook）
├─ installer/            # WiX 或 Inno Setup：驱动安装、任务计划注册
└─ docs/
```

---

## 7. 里程碑

| 阶段 | 内容 | 验收标准 |
|---|---|---|
| M1（1 周） | 原型：分层窗口 + DComp 半透明渲染 + CPU/GPU 使用率 + NVAPI GPU 温度 + 鼠标穿透/拖拽 | 右上角常显，内存 <30MB |
| M2（1 周） | 显隐策略：PresentMon 全屏检测 + SMTC 媒体检测，游戏显示/视频隐藏 | 三类全屏场景行为正确 |
| M3（1 周） | 硬盘温度（NVMe/SMART）；收起胶囊；**设置入口 = Ctrl+右键菜单**（无托盘）；透明度；开机自启；配置持久化 | 盘温显示、菜单可用、重启后自动运行 ✅ |
| M4（1 周） | CPU 温度/风扇转速：**lhm-bridge 无窗桥接进程**（LibreHardwareMonitorLib 官方库自建 20 行采集器，框架依赖发布仅 3.4MB，子进程自动重启）✅ | 单行含 CPU 温度+FAN 段、胶囊新增两页 ✅ |
| M5（2 周，可选） | 独占全屏 Hook OSD + 历史曲线 + 多显示器/多 GPU + 安装包 | 独占全屏游戏中可见 |

---

## 8. 风险与对策

| 风险 | 等级 | 对策 |
|---|---|---|
| WinRing0 驱动在部分新主板/Win11 24H2 读不到温度 | 高 | LHM 社区持续更新兜底；温度行自动隐藏；引导用户装厂商驱动 |
| 反作弊游戏误报注入 | 高 | 默认不注入，只提示无边框；Hook 功能默认关闭需用户显式开启 |
| ETW 会话权限（需管理员或的事件权限） | 中 | UI 进程普通权限运行，检测模块以分离的低权限 ETW 消费者实现（PresentMon 已验证可行）；不行则以 SMTC+前台窗口类名启发式兜底 |
| AMD/Intel 显卡覆盖率 | 中 | ADLX 优先，PDH GPU Engine 兜底，测不到显示 N/A |
| 多显卡（核显+独显） | 低 | 默认取"当前有负载的独显"，设置里可手动指定 |

---

## 9. 显式不做（保持低占用）
- 不做 Web 技术栈、不做换肤市场、不做云同步
- 默认不常驻网络（除可选检查更新，手动触发）
- 不采集任何用户数据，完全本地运行


---

## 开发完成状态（2026-09-12 最终版）

全部四期里程碑已完成并实机验证。最终功能清单：

### 功能
- **形态**：单行条（24px）/ 小胶囊（24px），等宽槽位（数值右对齐，宽度不随位数跳动），胶囊按页宽自适应
- **数据**：CPU 占用+温度、GPU 占用+温度、显存占用、内存占用、双盘温度（NVMe/SATA）、双风扇转速（lhm-bridge）
- **胶囊**：按硬件分组翻页（CPU/GPU/内存/硬盘/风扇/最高温度），2 秒轮播（1-5 秒可调），滚轮翻页，几何居中健康色点
- **色点四档**（阈值按本机校准）：红（温≥85°/载≥90%）橙（≥78°/≥80%）黄（≥68°/≥65%）绿（日常）
- **显隐策略**：永远置顶（前台钩子+每秒兜底），全屏看视频自动隐藏（SMTC+几何判定），其余场景常显
- **毛玻璃**：系统亚克力（SetWindowCompositionAttribute），深/浅两种雾面；普通模式区域裁剪圆角无投影
- **圆角**：普通模式 SetWindowRgn 裁剪（无投影）；毛玻璃模式 DWMWA_WINDOW_CORNER_PREFERENCE（系统圆角+雾面）
- **交互**：默认鼠标穿透；Ctrl 悬停解锁（低级键盘钩子即时响应）；Ctrl+左键拖拽（区分点击与拖拽，位置持久化）；Ctrl+右键全功能菜单（跟随鼠标弹出）；Ctrl+滚轮胶囊翻页
- **菜单**：形态切换、背景颜色（深/浅/毛玻璃×2）、透明度（25/50/70/90）、显示内容（5 段）、胶囊显示项（9 项）、轮播间隔（1/2/3/5s）、刷新频率（500ms/1s/2s）、开机自启、恢复吸附右上角、退出
- **可靠性**：三级进程（UI/采集/桥接）互相隔离，看门狗自动重启，作业对象绑定生命周期
- **持久化**：全部配置即改即存（%APPDATA%	empmon.conf），启动恢复

### 过程决策记录（踩坑与定案）
1. **CPU 温度源**：NVAPI 的 GetThermalSettings 在本机返回结构体版本不兼容（-9），改用 NVML（驱动自带 nvml.dll）——稳定
2. **TjMax 陷阱**：i5-13600KF 有"Distance to TjMax"传感器被 LHM 归为温度类，取最高值会误读（62° 实为余量 38°）。修复：排除 TjMax，优先 Package
3. **硬盘温度**：本机 Insider 预览版（28000）NVMe 协议查询返回全零、温度属性报 NOT_SUPPORTED，最终用 Get-StorageReliabilityCounter（PowerShell）60 秒轮询——免驱动全盘型覆盖
4. **CPU 温度/风扇**：WinRing0 内核驱动会被 360 拦截，弃用；改 lhm-bridge 无窗桥接进程（LibreHardwareMonitorLib，3.4MB）
5. **PresentMon 缓冲问题**：1.10 版 CSV 管道/文件运行期均不 flush（退出才落盘），流式实时 FPS 不可行，FPS 功能弃用
6. **色点阈值**：初版阈值在本机日常状态（NVMe 60°/内存 55%）就变橙黄，按本机基准校准四档
7. **无托盘设计**：所有设置入口收敛到 Ctrl+右键菜单（用户明确要求）

### 已知限制
- FPS 显示已弃用（PresentMon 缓冲问题，见决策 5）
- WebView 类应用（豆包等）的 GPU 数据可能归属子进程，偶尔不显示
- 内存温度未实现（多数消费级主板无此传感）
- 毛玻璃模式四角为系统圆角（约 8px），无法自定义更大弧度
