# 开发记录

## 2026-09-12

### M1：原型（完成）
- 搭建 Rust workspace：`tempmon-ui`（UI）+ `tempmon-sensor`（采集）
- 分层窗口 + Direct2D + UpdateLayeredWindow 逐像素透明
- CPU/内存（PDH）、GPU 占用与显存（PDH GPU Engine）、显存总量（DXGI）
- 双进程隔离：采集子进程 + 共享内存帧 + 看门狗（5 秒停滞自动重启）
- 实测：UI 进程私有内存 23MB，采集侧 ~52MB（含 NVIDIA 驱动模块）

### M2：显隐策略引擎（完成）
- 全屏前台检测：窗口矩形 == 显示器矩形
- SMTC（系统媒体会话）+ AUMID/进程名匹配 → 全屏看视频自动隐藏
- 视频暂停/退出全屏 → 恢复显示（用户实测确认）
- EVENT_SYSTEM_FOREGROUND 钩子即时夺回置顶 + 每秒兜底
- 全屏前台自动切 500ms 采样档（后按用户要求统一为固定刷新频率）

### M3：数据扩展 + 无托盘菜单（完成）
- 硬盘温度：NVMe 协议查询在本机 Insider 预览版返回全零（stornvme 缓冲问题），
  温度属性报 NOT_SUPPORTED；最终采用 Get-StorageReliabilityCounter
  （PowerShell 管道）60 秒低频轮询 + 缓存
- 收起小胶囊：健康色点（几何圆形，非字形）+ 分组翻页
- 无托盘设计：全部设置入口收敛到 Ctrl+右键菜单
- 菜单项：显示内容（5 段）、胶囊显示项（9 项）、轮播间隔（4 档）、
  刷新频率（3 档）、透明度（4 档）、背景颜色（4 模式）、开机自启、恢复吸附、退出
- 配置持久化：`%APPDATA%\tempmon.conf`，即改即存

### M4：CPU 温度 + 风扇转速（完成）
- WinRing0 内核驱动方案因 360 全家桶拦截风险弃用
- 改用 lhm-bridge：自建 20 行 C# 无窗采集器（LibreHardwareMonitorLib 0.9.4，
  框架依赖发布仅 3.4MB），采样 CPU Package 温度 + SuperIO 风扇
- 采集子进程拉起桥接、解析 stdout、崩溃 5 秒自动重启

### 踩坑记录

1. **NVAPI 结构体版本不兼容（-9）**
   `NvAPI_GPU_GetThermalSettings` 在 RTX 5070 Ti + 新驱动上返回
   NVAPI_INCOMPATIBLE_STRUCT_VERSION。改用 NVML（nvml.dll，驱动自带）
   的 `nvmlDeviceGetTemperature` —— 纯 C API 无版本问题。

2. **TjMax 传感器误读**
   i5-13600KF 的 "Distance to TjMax" 传感器被 LHM 归为温度类型，
   "取最高值"逻辑会把它当温度（62° 实为余量 38°）。
   修复：排除名字含 TjMax 的传感器，优先 Package。

3. **PresentMon 管道不吐数据**
   1.10 版 CSV 输出在运行期完全不 flush（stdout 管道/输出文件均然，
   退出时才一次性写出）。流式实时 FPS 不可行，短周期 `-timed 2
   -terminate_after_timed` 也因退出时不写文件而失败。FPS 功能弃用。

4. **stornvme 全零**
   Insider 预览版（build 28000）的 NVMe 协议查询返回全零数据，
   温度属性查询报 ERROR_NOT_SUPPORTED。零售版系统可能正常。

5. **亚克力层不理会窗口区域**
   毛玻璃雾面层铺满整个矩形窗口，自绘圆角/区域裁剪对它无效。
   毛玻璃模式改用 `DWMWA_WINDOW_CORNER_PREFERENCE = ROUND`
   （系统圆角，雾面层跟随）；普通模式仍用 SetWindowRgn（无投影）。

6. **SetWindowCompositionAttribute 未公开**
   不在 user32.lib 导入库中，需 GetProcAddress 运行时动态解析。

7. **窗口拖拽误触发展开**
   拖拽松手时 WM_LBUTTONUP 被当成"点击胶囊展开"。
   修复：记录按下时光标位置，位移 <6px 才算点击。

8. **拖拽中回弹**
   1 秒定时器在模态移动循环中仍执行渲染，用旧坐标拉回窗口。
   修复：WM_ENTERSIZEMOVE/WM_EXITSIZEMOVE 标记拖拽态，拖拽中跳过渲染。

9. **右键菜单被标题栏吞掉**
   Ctrl 态 WM_NCHITTEST 返回 HTCAPTION 会导致右键触发系统菜单。
   修复：返回 HTCLIENT，拖拽改为 WM_LBUTTONDOWN 里手动
   SendMessage(WM_NCLBUTTONDOWN) 触发。

### 性能参考值（本机实测）

| 进程 | 工作集 | 说明 |
|---|---|---|
| tempmon.exe (UI) | ~32MB | 渲染 + 策略 |
| tempmon.exe (sensor) | ~41MB | 含 NVIDIA 驱动用户态模块 |
| lhm-bridge.exe | ~75MB | .NET 运行时 + LHM |

### 待办（未实现）
- FPS 显示：需 PresentMon 库级集成或自研 ETW 消费者，当前弃用
- 内存温度：待有支持的硬件
- GPU 频率/功耗：可选扩展


## 发布（v1.0 · 2026-09-12）

- 分发目录：`dist/GLM桌面温度计/`（3.8MB，含启动脚本与使用说明）
- 压缩包：`dist/GLM桌面温度计-v1.0.zip`（1.5MB）
- 内容：tempmon.exe（带图标）+ lhm-bridge.exe + 2 个 Mono 运行时 dll + 启动脚本 + 说明
- 依赖：仅 .NET 8 Desktop Runtime（CPU 温度/风扇功能需要，缺失时自动降级）
- 已验证：从 dist 目录独立启动，UI/采集/桥接三级全部拉起（运行路径确认为 dist 下的 exe）


## 防遮挡自动避让系统（2026-09-14）

需求：温度计压住其他窗口的文字/图标时自动挪开，且不能带来卡顿。

### 最终方案
- **事件驱动**：SetWinEventHook 监听其他窗口 LOCATIONCHANGE / SHOW / HIDE /
  FOREGROUND（WINEVENT_SKIPOWNPROCESS 排除自身），700ms 节流后 PostMessage
  即时检测——屏幕静止时零轮询
- **两段式截屏**：请求阶段只设 WDA_EXCLUDEFROMCAPTURE（瞬时），
  45ms 定时器回调里 BitBlt（毫秒级）——UI 线程全程不睡眠，不卡鼠标
- **内容识别**：梯度边缘 ∪ 高色差像素（>60）一张内容图，全行只算一次、
  候选位置复用；40px 细扫描；文字=条带规则，图标=24×24 块密度（阈值 0.12）
- **三级选位**：右优先 → 左侧 → 最少遮挡兜底（粘性记忆 + 0.01 明显更优门槛 +
  4 秒冷却，防乒乓）；完全空位移动不受冷却限制
- **最宽形态预留**：所有翻页宽度每帧已排版算出，取最大值做占位计算
- **贴边自适应**：右缘放不下截尾优先左侧内容；位置整体夹回工作区

### 踩坑（本轮新增）
10. **DWM 系统圆角的投影去不掉**
    `DWMWA_NCRENDERING_POLICY=DISABLED`、`DWMWA_BORDER_COLOR=NONE` 都压不住
    Win11 圆角自带的投影；且毛玻璃雾面层（accent policy）不吃 `SetWindowRgn`
    区域裁剪。结论：毛玻璃=DWM 圆角+接受系统阴影，普通模式=rgn 裁剪（无投影）。

11. **WDA_EXCLUDEFROMCAPTURE 需要等待 DWM 生效**
    设置后立即 BitBlt 会截到自己。旧方案在 UI 线程 sleep(40ms) 等待——
    低级鼠标钩子同线程，表现为鼠标周期性卡顿。改为定时器两段式解决。

12. **`self.dctx.take()` 消费了跨阶段上下文**
    两阶段检测中第一阶段 take 掉 DodgeCtx，第二阶段拿到 None 静默返回——
    检测"正常"却永不避让。修复：DodgeCtx 全 Copy 字段，派生 Copy 只读借用。

13. **截屏排除的是窗口、不是投影**
    DWM 投影画在窗口矩形之外，WDA 排除窗口后阴影仍会进入截屏——
    对截屏内容判定时要记得这点（本轮阈值校准已考虑）。

## 2026-09-14（晚）：v1.2.0 稳定性大版本

用户报告"后台运行了很多 lhm-bridge.exe"（实测堆积 29 个、约 2.3GB 内存），
顺藤摸瓜做了一轮全面自检审计 + 三轮实机调试，最终挖出总根源。

### 根因链（ debugger 式复盘）
1. UI 看门狗判定 sensor 卡死用的是「连续 6 个 tick 帧号不变」；
2. **避让的淡出淡入动画每 16ms 手动调一次 tick()**——一次避让瞬间产生 20+ 个
   tick，此时 sensor 下一帧（周期 1s）还没到，帧号当然没变 → **每次避让必然
   误判卡死、杀掉 sensor 重拉**；
3. 旧版 sensor 被杀后 bridge（孙进程）变孤儿，永远存活——每次避让泄漏一个
   80MB 进程，用户频繁操作窗口时泄漏速度跟着窗口活动同步上升。

### 修复与加固（v1.2.0）
- 卡死判定改**时间基**：出过帧后帧停滞 >8s 才判卡死（与 tick 频率解耦）；
  从未出帧给 20s 冷启动余量
- 桥接挂入 sensor 的 `KILL_ON_JOB_CLOSE` Job：sensor 死，桥接必死
- sensor 重拉指数退避 6s→96s（连续失败才退避，正常重启不受影响）
- 共享内存序号改 u32 位模式存储（f32 槽 2²⁴ 后无法递增的隐患）+ 读侧撕裂检测
- LHM 缓存加 10s TTL：桥接 hang 死时隐藏温度而不是冻结旧值
- 单实例命名互斥体；开机自启迁 HKCU Run 注册表（schtasks ONLOGON 需管理员）
- 配置：pos=None 不再写 x=0/y=0（重启后钉在左上角的 bug）；capsule/row 补范围校验
- 修复配置键名不一致（UI 写 refresh=，sensor 读 refresh_ms=）导致菜单改刷新频率
  对采集侧从不生效
- 盘温优先原生 IOCTL（IOCTL_STORAGE_QUERY_PROPERTY / TempProp=13），
  部分盘返回 ERROR_NOT_SUPPORTED 时自动回退 PowerShell
- 诊断日志：TEMPMON_SENSOR_DEBUG / TEMPMON_DODGE_DEBUG 环境变量

### 踩坑（本轮新增）
14. **看门狗不能按 tick 计数**：UI 里任何会手动驱动 tick 的路径（动画、
    避让、滚轮）都会放大计数。健康判定一律用真实时间墙钟。
15. **子进程退出 ≠ 儿孙退出**：Job 对象是 Windows 上唯一可靠的进程树回收
    手段；spawn 失败（如 stdout take 失败）也要 kill 已创建的子进程。
16. **shared memory 序号别用 f32**：槽数据结构是 f32 就按位模式存 u32
    （to_bits/from_bits），否则 2²⁴ 帧后心跳停止。
17. **重构替换哨兵值时要追责初始语义**：stale_ticks=99 的隐含语义是
    "启动强制拉一次"，改成 None 后若不补 child.is_none() 分支，首次拉起
    会被跳过、传感器延迟 20s 才出现。

### 内存占用优化（同日追加）
- 实测三进程工作集 ~144MB → ~55-70MB（UI 41→7MB，桥接 72→17-31MB）
- 三进程每 10 分钟 `SetProcessWorkingSetSize` 修剪工作集（UI 悬停/拖拽时跳过）；
  桥接启动即修剪一次
- 桥接 GC 调优：工作站非并发 GC（ServerGC/ConcurrentGC 均关），堆更小
- 提交内存基本不变——收益体现在任务管理器显示的常驻工作集


## 2026-09-15

### 贴边归位（回归右缘）
需求：不遮挡时温度计尽量靠屏幕最右。
- 第一阶段未遮挡时，若曾挪离右缘（pos=Some 且右侧有空间）→ 转整行截屏；
- 第二阶段新增 ret_right 模式：只在完全空位中取 x 最大者，明显更靠右才动；
- 右缘 160px 窗口按钮区（caption_zone）照常避开，回位不压关闭按钮。

### 防乒乓（首版归位引发的震荡修复）
首版归位无阻尼：兜底巡检 2.5s 一次，内容稍变就挪，与遮挡避让形成
"挪走→归位→又挪走"循环。加三重阻尼：
1. 当前位连续 2 次检测空闲（内容稳定）才评估归位；
2. 归位收益 ≥100px 才动；
3. 任何移动（避让或归位）后 30s 冷却。
实测 4 分钟 83 次检测 0 移动，归位功能保留、移动低频克制。

教训：**任何"自动回归/自动优化"类逻辑都必须带阻尼**——检测频率远高于
环境真实变化频率时，无滞回的反馈系统必然振荡。

### 标题栏高度同步（同日追加）
需求：温度计悬在标题栏上沿显得突兀，应与软件右边的按钮保持同一高度。
- 借助自身穿透特性，`WindowFromPoint(温度计中点)` 直接取到宿主窗口；
- 用 `DwmGetWindowAttribute(DWMWA_CAPTION_BUTTON_BOUNDS)` 拿标题栏按钮的
  真实边界（不同软件高矮不一，比 45px 硬编码准），温度计垂直居中对齐按钮行；
- 桌面/任务栏（类名过滤）或拿不到按钮边界时不动作；±3px 死区防微动；
- 引入 `user_pinned`（手动 Ctrl 拖拽置位、随配置持久化）：钉住的位置不参与
  自动贴边与对齐，"恢复吸附右上角"解除。

### 标题栏同步调试（同日追加，三个坑）
1. `WindowFromPoint` 命中的常是内容**子窗口**（Edge 的
   `Chrome_RenderWidgetHostHWND`），DWM 属性对其全部失效——必须
   `GetAncestor(GA_ROOT)` 取顶层窗口；
2. `DWMWA_CAPTION_BUTTON_BOUNDS` 对自绘标题栏不可靠（Edge 报的值覆盖整个
   标签条）；且它是客户区坐标，直接加 GetWindowRect.top 会漏掉上方不可见
   边框（差 8~12px）。最终方案：`DWMWA_EXTENDED_FRAME_BOUNDS`（可见上缘）
   + Win11 标题栏按钮标准中心 24px，普适且稳定。
3. 每步加调试日志二分定位，比肉眼对截图快得多。

### 标题栏同步改为截图判定（按用户要求重做）
DWM/窗口 API 方案在不同软件上表现不一致，改为与避让同一套的截图判定：
- 每 ≥10s 截取右上角 200×48 条带（WDA 排除自身），计算每行边缘密度；
- 按钮行 = 第一处密度 >0.05 的行起、容忍 ≤4 行稀疏的连续带；取带中心，
  温度计垂直居中对齐（±3px 死区）；移动后 10s 冷却；
- 手动拖拽钉住（user_pinned，随配置持久化）的位置不参与。
实现教训：给 CapReq 加字段时用脚本批量改调用点漏了一处
（ret_right 分支的 begin_capture），编译器兜底抓到——批量改动后必须全量编译。

### 对齐参考全行 + 移动一步到位（同日追加）
- 对齐参考从"右缘 200px"扩为**整行**：检测条带改为整个工作区宽度 ×48px，
  左右两侧内容都参与行密度统计；
- 带边界检测（首末密集行）对密度阈值敏感、会跳变引发 y 抖动，改为
  **加权垂直中心**（每行边缘像素数加权平均，只统计上部 44 行）——连续平滑；
- 移动一步到位：避让/归位选定目标 x 时同步算出对齐 y，一次淡出斜向移动，
  不再"先平移、再上下挪"两段式；
- 行带改为按温度计所在行区间（wy0..wy0+rh）做遮挡分析，配合 48px 条带。
