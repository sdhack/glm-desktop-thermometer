# 开发记录

## 2026-09-12

### M1：原型（完成）
- 搭建 Rust workspace：`tempmon-ui`（UI）+ `tempmon-sensor`（采集）
- 分层窗口 + Direct2D + UpdateLayeredWindow 逐像素透明
- CPU/内存（PDH）、GPU 占用与显存（PDH GPU Engine）、显存总量（DXGI）
- 双进程隔离：采集子进程 + 共享内存帧 + 看门狗（5 秒停滞自动重启）
- 实测：UI 进程私有内存 23MB，采集侧 ~52MB（含 NVIDIA 驱动模块）

### M2：显隐策略引擎（完成）
- 全屏前台检测：窗口矩形 == 显示器矩形
- SMTC（系统媒体会话）+ AUMID/进程名匹配 → 全屏看视频自动隐藏
- 视频暂停/退出全屏 → 恢复显示（用户实测确认）
- EVENT_SYSTEM_FOREGROUND 钩子即时夺回置顶 + 每秒兜底
- 全屏前台自动切 500ms 采样档（后按用户要求统一为固定刷新频率）

### M3：数据扩展 + 无托盘菜单（完成）
- 硬盘温度：NVMe 协议查询在本机 Insider 预览版返回全零（stornvme 缓冲问题），
  温度属性报 NOT_SUPPORTED；最终采用 Get-StorageReliabilityCounter
  （PowerShell 管道）60 秒低频轮询 + 缓存
- 收起小胶囊：健康色点（几何圆形，非字形）+ 分组翻页
- 无托盘设计：全部设置入口收敛到 Ctrl+右键菜单
- 菜单项：显示内容（5 段）、胶囊显示项（9 项）、轮播间隔（4 档）、
  刷新频率（3 档）、透明度（4 档）、背景颜色（4 模式）、开机自启、恢复吸附、退出
- 配置持久化：`%APPDATA%\tempmon.conf`，即改即存

### M4：CPU 温度 + 风扇转速（完成）
- WinRing0 内核驱动方案因 360 全家桶拦截风险弃用
- 改用 lhm-bridge：自建 20 行 C# 无窗采集器（LibreHardwareMonitorLib 0.9.4，
  框架依赖发布仅 3.4MB），采样 CPU Package 温度 + SuperIO 风扇
- 采集子进程拉起桥接、解析 stdout、崩溃 5 秒自动重启

### 踩坑记录

1. **NVAPI 结构体版本不兼容（-9）**
   `NvAPI_GPU_GetThermalSettings` 在 RTX 5070 Ti + 新驱动上返回
   NVAPI_INCOMPATIBLE_STRUCT_VERSION。改用 NVML（nvml.dll，驱动自带）
   的 `nvmlDeviceGetTemperature` —— 纯 C API 无版本问题。

2. **TjMax 传感器误读**
   i5-13600KF 的 "Distance to TjMax" 传感器被 LHM 归为温度类型，
   "取最高值"逻辑会把它当温度（62° 实为余量 38°）。
   修复：排除名字含 TjMax 的传感器，优先 Package。

3. **PresentMon 管道不吐数据**
   1.10 版 CSV 输出在运行期完全不 flush（stdout 管道/输出文件均然，
   退出时才一次性写出）。流式实时 FPS 不可行，短周期 `-timed 2
   -terminate_after_timed` 也因退出时不写文件而失败。FPS 功能弃用。

4. **stornvme 全零**
   Insider 预览版（build 28000）的 NVMe 协议查询返回全零数据，
   温度属性查询报 ERROR_NOT_SUPPORTED。零售版系统可能正常。

5. **亚克力层不理会窗口区域**
   毛玻璃雾面层铺满整个矩形窗口，自绘圆角/区域裁剪对它无效。
   毛玻璃模式改用 `DWMWA_WINDOW_CORNER_PREFERENCE = ROUND`
   （系统圆角，雾面层跟随）；普通模式仍用 SetWindowRgn（无投影）。

6. **SetWindowCompositionAttribute 未公开**
   不在 user32.lib 导入库中，需 GetProcAddress 运行时动态解析。

7. **窗口拖拽误触发展开**
   拖拽松手时 WM_LBUTTONUP 被当成"点击胶囊展开"。
   修复：记录按下时光标位置，位移 <6px 才算点击。

8. **拖拽中回弹**
   1 秒定时器在模态移动循环中仍执行渲染，用旧坐标拉回窗口。
   修复：WM_ENTERSIZEMOVE/WM_EXITSIZEMOVE 标记拖拽态，拖拽中跳过渲染。

9. **右键菜单被标题栏吞掉**
   Ctrl 态 WM_NCHITTEST 返回 HTCAPTION 会导致右键触发系统菜单。
   修复：返回 HTCLIENT，拖拽改为 WM_LBUTTONDOWN 里手动
   SendMessage(WM_NCLBUTTONDOWN) 触发。

### 性能参考值（本机实测）

| 进程 | 工作集 | 说明 |
|---|---|---|
| tempmon.exe (UI) | ~32MB | 渲染 + 策略 |
| tempmon.exe (sensor) | ~41MB | 含 NVIDIA 驱动用户态模块 |
| lhm-bridge.exe | ~75MB | .NET 运行时 + LHM |

### 待办（未实现）
- FPS 显示：需 PresentMon 库级集成或自研 ETW 消费者，当前弃用
- 内存温度：待有支持的硬件
- GPU 频率/功耗：可选扩展


## 发布（v1.0 · 2026-09-12）

- 分发目录：`dist/GLM桌面温度计/`（3.8MB，含启动脚本与使用说明）
- 压缩包：`dist/GLM桌面温度计-v1.0.zip`（1.5MB）
- 内容：tempmon.exe（带图标）+ lhm-bridge.exe + 2 个 Mono 运行时 dll + 启动脚本 + 说明
- 依赖：仅 .NET 8 Desktop Runtime（CPU 温度/风扇功能需要，缺失时自动降级）
- 已验证：从 dist 目录独立启动，UI/采集/桥接三级全部拉起（运行路径确认为 dist 下的 exe）


## 防遮挡自动避让系统（2026-09-14）

需求：温度计压住其他窗口的文字/图标时自动挪开，且不能带来卡顿。

### 最终方案
- **事件驱动**：SetWinEventHook 监听其他窗口 LOCATIONCHANGE / SHOW / HIDE /
  FOREGROUND（WINEVENT_SKIPOWNPROCESS 排除自身），700ms 节流后 PostMessage
  即时检测——屏幕静止时零轮询
- **两段式截屏**：请求阶段只设 WDA_EXCLUDEFROMCAPTURE（瞬时），
  45ms 定时器回调里 BitBlt（毫秒级）——UI 线程全程不睡眠，不卡鼠标
- **内容识别**：梯度边缘 ∪ 高色差像素（>60）一张内容图，全行只算一次、
  候选位置复用；40px 细扫描；文字=条带规则，图标=24×24 块密度（阈值 0.12）
- **三级选位**：右优先 → 左侧 → 最少遮挡兜底（粘性记忆 + 0.01 明显更优门槛 +
  4 秒冷却，防乒乓）；完全空位移动不受冷却限制
- **最宽形态预留**：所有翻页宽度每帧已排版算出，取最大值做占位计算
- **贴边自适应**：右缘放不下截尾优先左侧内容；位置整体夹回工作区

### 踩坑（本轮新增）
10. **DWM 系统圆角的投影去不掉**
    `DWMWA_NCRENDERING_POLICY=DISABLED`、`DWMWA_BORDER_COLOR=NONE` 都压不住
    Win11 圆角自带的投影；且毛玻璃雾面层（accent policy）不吃 `SetWindowRgn`
    区域裁剪。结论：毛玻璃=DWM 圆角+接受系统阴影，普通模式=rgn 裁剪（无投影）。

11. **WDA_EXCLUDEFROMCAPTURE 需要等待 DWM 生效**
    设置后立即 BitBlt 会截到自己。旧方案在 UI 线程 sleep(40ms) 等待——
    低级鼠标钩子同线程，表现为鼠标周期性卡顿。改为定时器两段式解决。

12. **`self.dctx.take()` 消费了跨阶段上下文**
    两阶段检测中第一阶段 take 掉 DodgeCtx，第二阶段拿到 None 静默返回——
    检测"正常"却永不避让。修复：DodgeCtx 全 Copy 字段，派生 Copy 只读借用。

13. **截屏排除的是窗口、不是投影**
    DWM 投影画在窗口矩形之外，WDA 排除窗口后阴影仍会进入截屏——
    对截屏内容判定时要记得这点（本轮阈值校准已考虑）。
